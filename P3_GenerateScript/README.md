# RNN Project: generate TV script

## Folder Structure
```python
├── use_pytorch
│   ├── data  # training data (not publicly shared)
│   ├── dlnd_tv_script_generation.ipynb  # main Jupyter notebook using Pytorch
│   ├── helper.py  # common Python function such as loading data, save the model
│   └── problem_unittests.py  # functions for unittestings
└── use_tensorflow
    ├── dlnd_tv_script_generation.ipynb  # main Jupyter notebook using Tensorflow
    ├── helper.py  # common Python function such as loading data, save the model
    ├── problem_unittests.py  # functions for unittestings
    └── simpsons  # training data (not publicly shared)
```
